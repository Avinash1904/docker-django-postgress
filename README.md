# docker-django-postgress

```
1. docker build .
2. docker-compose up --build
3. docker-compose up -d  --> to run in background
4. docker-compose down --> to bring down docker
5. docker-compose logs
6. docker-compose exec <service name> command
  eg. docker-compose exec web python manage.py migrate

```
